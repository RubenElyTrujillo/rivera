/**
 * @deprecated Importa directamente desde "@/domain/types".
 * Este archivo existe solo para compatibilidad durante la migración.
 */
export type {
  IHeroContent,
  IService,
  IMaterial,
  IMaterialFinish,
  ISpaceProject,
  ICatalogContent,
  IContactInfo,
  IFooterContent,
  ISeoSettings,
  IMedia,
  JwtPayload,
  IPageData,
} from "@/domain/types";
